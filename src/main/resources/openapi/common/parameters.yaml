openapi: 3.0.3
info:
  title: common parameters
  version: 1.0.0
  description: parameters just for our API's

components:
  parameters:
    DietIdParam:
      name: diet_id
      in: query
      required: true
      schema:
        type: integer
        format: int64
      example: 1
    CategoryIdListParam:
      name: category_id_list
      in: query
      style: form
      required: false
      explode: true
      schema:
        type: array
        items:
          type: integer
          format: int64
        example: [ 1, 2, 3 ]
    MealIdParam:
      name: id
      in: query
      required: true
      schema:
        type: integer
        format: int64
      example: 1
    IngredientIdParam:
      name: id
      in: query
      required: true
      schema:
        type: integer
        format: int64
      example: 1
    AllergyIdListParam:
      name: allergy_id_list
      in: query
      style: form
      required: false
      explode: true
      schema:
        type: array
        items:
          type: integer
          format: int64
        example: [ 1, 2, 3 ]
